# Lisp Playground — 公開後確認ログ

**作成日**: 2026年4月29日  
**対象公開先**: GitHub Pages  
**公開 URL**: https://mr-129.github.io/lisp-playground/  
**対象コミット**: `be6e67d` (`fix: improve mobile layout`)

---

## 1. 結論

- GitHub Pages へのデプロイは成功した
- 主要導線の公開後スモークテストは通過した
- 公開環境での localStorage 保存と復元も確認できた
- 公開環境で 38 問すべての解答総当たり実行が通過した
- 公開環境で 390px / 320px 幅のモバイル表示確認も通過した
- 学習ページ、エディタ、REPL の主要導線では、現時点で致命的な不具合は確認していない

---

## 2. 実施した確認

| 区分 | 確認内容 | 結果 | 補足 |
|---|---|---|---|
| デプロイ | GitHub Actions `Build & Deploy` | PASS | `main` への push 後に success を確認 |
| 公開設定 | GitHub Pages 設定と公開 URL | PASS | `https://mr-129.github.io/lisp-playground/` で公開中 |
| 到達性 | `/`, `/#/editor`, `/#/repl` の表示 | PASS | 主要ルートで表示を確認 |
| 学習ページ | ウェルカム画面、問題一覧、最初の問題選択 | PASS | 最初の問題「初めてのS式」を表示できた |
| 学習ページ | 解答表示 | PASS | 問題詳細から解答表示を確認 |
| エディタ | 学習ページからの遷移 | PASS | 「エディタで解く」で `/#/editor` へ遷移 |
| エディタ | 表示中の解答コード実行 | PASS | 正解バッジ表示、出力 `30\n30\n`、戻り値 `30` を確認 |
| localStorage | 正答後の保存 | PASS | `lisp-playground-problem-id=basic-01`, solved IDs に `basic-01` を確認 |
| localStorage | 再読込後のエディタ復元 | PASS | `📝 初めてのS式` と解答コードの復元を確認 |
| localStorage | 再読込後の学習ページ復元 | PASS | 選択中問題 `初めてのS式` の復元を確認 |
| 問題データ | 38 問の順次実行確認 | PASS | 38/38 問で正解判定、fail=0 を確認 |
| モバイル表示 | 390px 幅で learn / editor / repl を確認 | PASS | 横スクロールなし、主要操作ボタン表示を確認 |
| モバイル表示 | 320px 幅で learn / editor / repl を確認 | PASS | ヘッダー、CTA、出力領域のオーバーフロー解消を確認 |
| REPL | `(+ 1 2 3)` の評価 | PASS | 結果 `6` を確認 |
| REPL | クリア操作 | PASS | 初期の案内表示へ戻ることを確認 |

---

## 3. 実施方法

### 3.1 デプロイ確認

- GitHub CLI で `Build & Deploy` workflow の success を確認
- GitHub Pages API で公開 URL と公開方式が workflow ベースであることを確認

### 3.2 ルート到達性確認

- 公開 URL のトップ画面を取得し、学習ページの主要文言を確認
- `/#/editor` と `/#/repl` を直接取得し、各ページ固有の文言が表示されることを確認

### 3.3 公開後スモークテスト

Playwright + Chromium の headless 実行で、次の操作を順に確認した。

1. トップページを開く
2. 問題一覧の最初の問題を選択する
3. 解答を表示する
4. 「エディタで解く」でエディタページへ遷移する
5. 解答コードを実行し、正解バッジと実行結果を確認する
6. REPL ページへ移動する
7. `(+ 1 2 3)` を評価して `6` を確認する
8. クリア操作後に初期表示へ戻ることを確認する

### 3.4 localStorage 保存・復元確認

同じく公開 URL 上で、次の保存と復元を確認した。

1. `localStorage.clear()` 後に最初の問題 `basic-01` を選択する
2. 解答を表示し、エディタで正答コードを実行する
3. `lisp-playground-problem-id` が `basic-01` として保存されることを確認する
4. `lisp-playground-solved-problem-ids` に `basic-01` が保存されることを確認する
5. `lisp-playground-code` に解答コード `(print (* 5 6))` を含むことを確認する
6. `/#/editor` を再読込し、現在の問題ラベルとエディタ内容が復元されることを確認する
7. `/` を開き直し、学習ページでも選択中問題が復元されることを確認する

### 3.5 問題データ総当たり確認

同じく公開 URL 上で、問題一覧に表示される全 38 問について次を順に確認した。

1. 問題を選択する
2. 解答を表示する
3. エディタページへ遷移する
4. 解答コードを実行する
5. `✓ 正解！` バッジが表示され、`✗ 不正解` や実行エラーが出ないことを確認する

結果は 38/38 問が通過し、fail=0 だった。

### 3.6 モバイル表示確認

公開 URL 上で Playwright + Chromium を使い、390px 幅と 320px 幅の 2 パターンを確認した。

1. `/` を開き、学習ページのヘッダー、問題導線、フリーモード導線が viewport 内に収まることを確認する
2. `/#/editor` を開き、ヘッダー、実行ボタン、出力領域が横スクロールなしで表示されることを確認する
3. `/#/repl` を開き、ナビゲーション、入力領域、実行結果表示が viewport 内に収まることを確認する

結果は 390px / 320px の両方で学習ページ、エディタ、REPL が通過した。

---

## 4. 今回の確認で未カバーの範囲

- CodeMirror 上での手入力、ショートカット、編集体験の詳細確認
- ブラウザ差分を含むクロスブラウザ確認

---

## 5. 次に行う確認候補

1. クロスブラウザ確認
2. CodeMirror の手入力編集とショートカット確認